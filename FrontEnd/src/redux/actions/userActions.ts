
export const userSliceNamespace = 'users';

export const userActionNames = {
    LOGIN: `${userSliceNamespace}/login`,
    SET_TOKEN: `${userSliceNamespace}/setToken`,
    REMOVE_TOKEN: `${userSliceNamespace}/removeToken`,
};